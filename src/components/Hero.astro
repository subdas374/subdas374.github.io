---
---

<section id="home" class="min-h-screen flex items-center justify-center pt-16 px-4 bg-gradient-to-br from-blue-50 via-white to-teal-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900">
  <div class="container mx-auto text-center max-w-5xl">
    <div class="mb-8 animate-fade-in">
      <div class="w-28 h-28 sm:w-36 sm:h-36 mx-auto mb-6 rounded-full bg-gradient-to-br from-primary-700 via-secondary-600 to-primary-600 flex items-center justify-center text-white text-4xl sm:text-5xl font-bold shadow-2xl ring-4 ring-white dark:ring-gray-800">
        SD
      </div>
    </div>
    
    <h1 class="text-4xl sm:text-5xl md:text-7xl font-bold text-gray-900 dark:text-white mb-4 animate-fade-in-delay bg-gradient-to-r from-primary-700 to-secondary-600 bg-clip-text text-transparent">
      Subrata Das
    </h1>
    
    <p class="text-xl sm:text-2xl md:text-3xl text-primary-700 dark:text-primary-400 mb-3 font-semibold animate-fade-in-delay">
      Hardware & Networking Student
    </p>
    
    <p class="text-base sm:text-lg md:text-xl text-gray-600 dark:text-gray-300 mb-10 max-w-3xl mx-auto px-4 leading-relaxed animate-fade-in-delay">
      Passionate about network infrastructure, system administration, and building robust IT solutions
    </p>
    
    <div class="flex flex-col sm:flex-row gap-4 justify-center px-4 animate-fade-in-delay">
      <a href="#projects" class="group px-8 py-4 bg-primary-700 hover:bg-primary-800 text-white rounded-xl transition-all transform hover:scale-105 shadow-lg hover:shadow-2xl font-semibold">
        <span class="flex items-center justify-center gap-2">
          View Projects
          <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
        </span>
      </a>
      <a href="/resume.pdf" download class="px-8 py-4 bg-secondary-600 hover:bg-secondary-700 text-white rounded-xl transition-all transform hover:scale-105 shadow-lg hover:shadow-2xl font-semibold flex items-center justify-center gap-2">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
        </svg>
        Download CV
      </a>
      <a href="#contact" class="px-8 py-4 border-2 border-primary-700 text-primary-700 dark:text-primary-400 dark:border-primary-400 rounded-xl hover:bg-primary-50 dark:hover:bg-gray-800 transition-all transform hover:scale-105 font-semibold">
        Contact Me
      </a>
    </div>
  </div>
</section>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 0.8s ease-out;
  }

  .animate-fade-in-delay {
    animation: fadeIn 0.8s ease-out 0.3s both;
  }
</style>
